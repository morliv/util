from setuptools import setup, find_packages

setup(
    name="util",
    version="1.0.2",
    author="",
    description="",
    packages=find_packages(),
    install_requires=[
        "watchdog"
    ],
    entry_points={},
)
