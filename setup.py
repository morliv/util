from setuptools import setup, find_packages

setup(
    name="util",
    version="1.0.0",
    author="",
    description="",
    packages=find_packages(),
    install_requires=[],
    entry_points={},
)
